import { raceReducer } from '../slices/race';
import { winnerReducer } from '../slices/winner';
import { carReducer } from '../slices/car';

const reducer = {
  raceReducer,
  winnerReducer,
  carReducer,
};

export default reducer;
