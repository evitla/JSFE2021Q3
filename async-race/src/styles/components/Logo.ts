import styled from 'styled-components';

const Logo = styled.div`
  display: flex;
  align-items: center;
  column-gap: 0.75rem;

  img {
    width: 100px;
  }

  h1 {
    font-size: 1.625rem;
  }
`;

export default Logo;
